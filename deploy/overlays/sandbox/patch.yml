apiVersion: apps/v1
kind: Deployment
metadata:
  name: odya-api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    spec:
      containers:
        - name: odya-api
